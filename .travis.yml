language: python
python:
  - "2.7"

before_install:
  - sudo apt-get install gfortran

install:
  - pip install numpy
  - pip install matplotlib
  - pip install scipy
  - python setup.py install

script:  py.test
